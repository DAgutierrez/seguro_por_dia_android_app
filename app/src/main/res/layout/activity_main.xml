<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/camera_gradient_background"
    android:gravity="center"
    android:padding="32dp">

    <!-- App Title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="📱 Seguro por el Día"
        android:textColor="@color/white"
        android:textSize="32sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Detección de Objetos con IA"
        android:textColor="@color/semi_transparent_white"
        android:textSize="16sp"
        android:layout_marginBottom="48dp" />

    <!-- Navigation Buttons Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center">

        <!-- Camera View Button -->
        <Button
            android:id="@+id/btn_camera_view"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:text="📷 Abrir Cámara"
            android:textColor="@color/white"
            android:textSize="18sp"
            android:textStyle="bold"
            android:background="@drawable/camera_button_background"
            android:layout_marginBottom="24dp"
            android:drawableStart="@drawable/ic_camera"
            android:drawablePadding="16dp" />

           

        <!-- Camera List Button -->
        <Button
            android:id="@+id/btn_camera_list"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:text="📋 Lista de Cámaras"
            android:textColor="@color/white"
            android:textSize="18sp"
            android:textStyle="bold"
            android:background="@drawable/camera_button_background"
            android:drawableStart="@drawable/ic_camera_list"
            android:drawablePadding="16dp" />

        <!-- Inspection Button -->
        <Button
            android:id="@+id/btn_inspection"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:text="🧾 Inspección Vehicular"
            android:textColor="@color/white"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_marginTop="24dp"
            android:background="@drawable/camera_button_background"
            android:drawableStart="@drawable/ic_camera_list"
            android:drawablePadding="16dp" />

    </LinearLayout>

    <!-- App Info -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Versión 1.0 - Android Nativo con Kotlin"
        android:textColor="@color/semi_transparent_white"
        android:textSize="12sp"
        android:layout_marginTop="48dp" />

</LinearLayout>